"""Neural network architectures for reinforcement learning agents.

This package defines the specific neural network models used by various RL
agents within the smart building control system. These networks typically
include actor networks (for policy representation) and critic networks (for
value function approximation).

Modules:
  sac_networks: Contains the actor and critic network definitions tailored for
    the Soft Actor-Critic (SAC) agent.
"""
