"""Reinforcement learning agents and associated network architectures.

This package provides implementations of reinforcement learning (RL) agents,
such as Soft Actor-Critic (SAC), that are tailored for smart building control.
It also includes the neural network models that form the core of these agents'
decision-making processes (e.g., actor and critic networks).

Modules:
  networks: Defines the neural network architectures used by the agents.
  sac_agent: Implements the Soft Actor-Critic agent.
"""
