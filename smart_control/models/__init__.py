"""Base classes for smart building control models.

This package defines the abstract base classes that form the core components
of the smart building control system. These include models for the building
itself, energy cost calculation, data normalization, occupancy simulation, and
reward function definition.

Each base class outlines an interface that concrete implementations must adhere
to, ensuring modularity and interchangeability within the control framework.
"""
